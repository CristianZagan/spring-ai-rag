spring:
  application:
    name: spring-ai-rag
  ai:
    openai:
      api-key: ${OPENAI_API_KEY}
      embedding:
        model: text-embedding-3-small

sfg:
  aiapp:
    vectorStorePath: C:/Users/crist/Downloads/tmp/vectorstore.json
    documentsToLoad:
      - classpath:/movies500.csv
